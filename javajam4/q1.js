let city = "Kuala Lumpur"; 
function travel(){ 
let city = "Johor Bahru"; 
console.log(city); 
} 
travel(); 

//it will print johor bahru because the variable city is declared inside the function travel 
// and it will take precedence over the global variable city.